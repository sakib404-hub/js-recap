console.log("Hellow world!");
/**
 * ? sometime the situation gets like we have to store multiple value in a single variable this is where the concepts of array comes from
 * ? There are always situations where we would be needing array like storing multiple values having same data type
 */

const numbers = [12, 22, 767, 79];
const friends = ["abdul", "sattar", "karim", "jabbar", "niloy"]; //? array containg only string value
const passed = [true, false, true, false]; //? array containing multiple boolean value
const mixedArray = [12, "Khagan Biruliya", true, 45.88]; //? we can declear mixed array but it is of no use or less optimized therefore we will be not using it
console.log(numbers);
console.log(numbers.length, friends.length);
