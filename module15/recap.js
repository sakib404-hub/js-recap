/**
 * ? var --> which was used in ancient time now a days it is not used and it is function scope
 * ? const --> value will not change , we have to assign value while declearing. it is a block scoped ES6 js feature
 * ? let --> value can be changed or reassigned . it is also block scoped ES6 feature
 */
