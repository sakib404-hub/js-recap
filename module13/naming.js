/**
 * ! These are the rules while writing a varibale name
 * ? while writing varibale name we should keep in mind that no keyword can be used
 * ? variable name should be in a single word without any space or other thing
 * ? we can use underscore if we want
 * ? no number can be used in the first character while declearing a variable.Meaning can not start with a number but later we can use number
 * ? Varibale name is case sensetive
 * ? variable naming keyword is case sensetive therefore you can not write var in place of VAR
 */

/**
 * ! Variable naming convention
 * ? Snake Case uses underscore in the gap while declearing a variable(my_current_home_address)
 * ? Camel case uses uppercase latter while writing another meaningfull word (myCurrentHomeAddress)
 * ? Pascal Case uses the Camel case but the first character will also be in uppercase like  (MyCurrentHomeAddress) we will not be using this while declearing varibale we will be using it for declearing classes and React Component Name
 */

//! Reserved keywords can not be used while declearing a variable

var my_current_home_address = "Andor Killa Bandorban with some sundarban";
var myCurrentHomeAddress = "Andor Killa Bandorban with some sundarban";
var MyCurrentHomeAddress = "Andor Killa Bandorban with some sundarban";
