/**
 * ? VARIABLE -- is nothing but container
 */

//? number type variable
var sunglassPrice = 80;
console.log(sunglassPrice);
console.log(typeof sunglassPrice);

//? string type variable
var studyDept = "Computer Science and Engineering";
console.log(studyDept);
console.log(typeof studyDept);

//? Boolean
var passed = true;
console.log(passed);
console.log(typeof passed);

/**
 * ? Naming Conventin
 * ? signle word
 * ? no quotes
 * ? no gap or dash
 * ? can not start with a number
 * ? can not be a keyword
 * ? we will be writing this in camel case
 */

var lastExamMark = 89;
console.log(lastExamMark);
