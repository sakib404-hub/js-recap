/**
 * Comparison Operator
 * ? Bigger >
 * ? Lesser <
 * ? Equal ==, ===
 * ? Equal or Bigger >= -- greater then or equals to
 * ? Equal or Lesser <= -- less then or equal to
 * ? Not Equal !=, !==
 * ------------------------------------------
 * ? and or logical and &&
 * ? or or logical or ||
 * ? not or logical not !
 * ? ternary operator ?
 */
